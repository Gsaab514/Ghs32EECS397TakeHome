1.) The files are in the directory named "createfile".

2.) For the URL name extractions it's in the directory "urlname".

3.) For the CGI answers the code is located within the "cgi" directory.

4.) For the csv to tsv answers, the code is located within the "tsv" directory. 

5.) For the meta-programming code, the code is located within the "date" directory. 


The bash, awk and php code are located in subdirectories with those respective names. 